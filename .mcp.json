{
  "mcpServers": {
    "fractary-codex": {
      "command": "bash",
      "args": ["-c", "export $(cat .env | grep -v '^#' | xargs) && npx -y @fractary/codex-mcp --config .fractary/codex/config.yaml"]
    }
  }
}
